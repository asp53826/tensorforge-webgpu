name: Autonomous Engineering Lab

on:
  schedule:
    - cron: "17 9 * * 4"
  workflow_dispatch:
  pull_request_target:
    types: [opened, synchronize, reopened]

permissions:
  contents: read

concurrency:
  group: autonomous-lab-${{ github.event_name }}
  cancel-in-progress: true

jobs:
  repo-medic:
    if: github.event_name != 'pull_request_target'
    permissions:
      contents: read
    uses: asp53826/portfolio-ops/.github/workflows/repo-medic.yml@v1.0.0
    with:
      runtime: node
      runtime-version: "22"
      install-command: >-
        npm ci
      test-command: >-
        npm run check && npm run build

  documentation-scribe:
    if: github.event_name != 'pull_request_target'
    permissions:
      contents: read
      issues: write
    uses: asp53826/portfolio-ops/.github/workflows/documentation-scribe.yml@v1.0.0

  demo-watchtower:
    if: github.event_name != 'pull_request_target'
    permissions:
      contents: read
      issues: write
    uses: asp53826/portfolio-ops/.github/workflows/demo-watchtower.yml@v1.0.0
    with:
      urls: >-
        https://asp53826.github.io/tensorforge-webgpu/

  dependency-caretaker:
    if: github.event_name == 'pull_request_target' && github.actor == 'dependabot[bot]'
    permissions:
      contents: write
      pull-requests: write
    uses: asp53826/portfolio-ops/.github/workflows/dependency-caretaker.yml@v1.0.0

